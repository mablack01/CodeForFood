# Ignores node_module folder and the package-lock.json matter where they are
node_modules/*
package-lock.json
*/node_modules/*
*/package-lock.json

# Ignores .swp files in the current directory. Swap files are used by text editors when a file is open
*.swp

# Ignores the data files (both the beautified and non beautified versions) for those of you who have them
/datafiles/*
umass-export-full.json
full-data-beautified.json
*/umass-export-full.json
*/full-data-beautified.json
